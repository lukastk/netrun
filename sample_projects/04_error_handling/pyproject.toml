[project]
name = "error-handling"
version = "0.1.0"
description = "Error handling examples: retries, timeouts, dead letter queue, type checking"
requires-python = ">=3.11"
dependencies = [
    "netrun",
    "netrun-ui",
]

[tool.uv.sources]
netrun = { path = "../../netrun", editable = true }
netrun-ui = { path = "../../netrun-ui", editable = true }
