[project]
name = "run-to-targets-example"
version = "0.1.0"
description = "Demonstrates run_to_targets for testing individual nodes in a pipeline"
requires-python = ">=3.11"
dependencies = [
    "jupyterlab>=4.5.3",
    "netrun",
    "netrun-ui",
]

[tool.uv.sources]
netrun = { path = "../../netrun", editable = true }
netrun-ui = { path = "../../netrun-ui", editable = true }
