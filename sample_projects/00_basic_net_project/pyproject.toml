[project]
name = "basic-net-project"
version = "0.1.0"
description = "A basic example of using netrun"
requires-python = ">=3.11"
dependencies = [
    "netrun",
    "netrun-ui",
]

[tool.uv.sources]
netrun = { path = "../../netrun" }
netrun-ui = { path = "../../netrun-ui" }
